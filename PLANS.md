# ExecPlan

## 任务
- 修改第二幕总汇报构建逻辑，解决用户指出的最终成品问题：
  - 封面页过于花哨、不正式；
  - 目录页不需要；
  - 各问题过渡页标题不规范，且副标题是自行总结，不应保留；
  - `Q4` 的“补充过渡页”需要删除；
  - 自制 HTML 截图页导入 PPT 后出现左、右、下侧黑边。

## 目标拆解
1. 以 `Case1-第二幕.md` 为真源，提取问题 1 到问题 6 的完整原文。
2. 调整单页 HTML 导出模式，确保截图时：
   - 页面以 1920×1080 原尺寸导出；
   - 不发生页面居中缩放；
   - 不保留阴影、提示控件和外围深色背景；
   - 最终 PNG 没有黑边。
3. 重写总稿支持页生成逻辑：
   - 封面只保留正式标题和全体组员姓名；
   - 删除目录页；
   - 所有问题页只显示“问题X + 问题原文”，不显示“问题过渡”与灰色副标题；
   - 删除 `Q4-补充` 过渡页。
4. 更新总稿顺序配置并重新构建：
   - 重新导出自制 PNG；
   - 重新生成支持页 pptx；
   - 重新合并最终总稿 pptx。
5. 进行结果验证：
   - 检查封面、Q1/Q4/Q6 过渡页文本是否准确；
   - 检查目录页和 `Q4-补充` 页是否消失；
   - 检查自制图片页是否无黑边；
   - 更新 `PROGRESS.md` 并提交。

## 执行约束
- 不修改 `同学ppt/` 内任何原始文件。
- 不引入新的汇报逻辑和“主讲/补充”层级表述。
- 问题标题必须与 `Case1-第二幕.md` 开头原文保持一致。
- 黑边问题必须通过重新导出图片和复检来真正解决，不能只在 PPT 里裁边掩盖。

## 当前状态
- 已完成：总稿构建链路已跑通，已生成过一版总汇报 `pptx`；已确认当前问题集中在支持页文案和 HTML 截图导出方式。
- 正在做：重写导出模式与支持页生成逻辑。
- 下一步：先改 `PPT-单页脚本.js` 的导出模式，再改 `build_support_deck.js` 与 `deck_order.json`，随后重建并验证。

# ExecPlan

## 任务
- 按老师临时要求，为第二幕总汇报新增两张纯文字补充页，内容来源于 `新建 DOCX 文档.docx` 中刷蓝 / 刷红的文字：
  - 鉴别诊断页：放在诊断标准（Q3）后面；
  - 病因 / 基本机制 / 诱因 / 高危因素页：放在一级、二级预防前面；
- 维持现有总稿风格，不改同学原稿，不扩写细节，只做“课堂讨论提示型”文字页。

## 目标拆解
1. 读取 `新建 DOCX 文档.docx`，提取刷蓝 / 刷红文字，并按主题归类。
2. 明确两张新页的结构：
   - 第一张：鉴别诊断；
   - 第二张：病因、基本机制、诱因、高危因素。
3. 修改 `build_support_deck.js`：
   - 新增两张支持页；
   - 保持当前第二幕支持页的视觉风格；
   - 文案做简洁的课堂讨论提示，不堆长段。
4. 修改 `deck_order.json`：
   - 鉴别诊断页插到 `image_q3` 后、`section_q4` 前；
   - 病因/机制/诱因/高危因素页插到 `section_q6` 后、同学的一级/二级预防页前。
5. 重建总稿并验证新增两页位置、标题、版式是否正确。
6. 更新 `PROGRESS.md` 并提交。

## 执行约束
- 不修改 `同学ppt/` 内任何原始文件。
- 不扩写成大段正文，只保留适合课堂口头展开的关键词和短句。
- 内容必须以用户提供的 Word 标注为准，不自行补造新论点。

## 当前状态
- 已完成：第二幕总稿基础构建链路已稳定；封面、问题标题页、页脚恢复方案均已落实。
- 正在做：读取 Word 标注内容，并把两张补充页插入现有总稿顺序。
- 下一步：抽取刷蓝 / 刷红文字后，直接落到 `build_support_deck.js` 和 `deck_order.json` 中实现。

# ExecPlan

## 任务
- 汇总 `血液与肿瘤/PBL作业/Case1-第二幕/同学ppt/` 中 9 位同学的原始 `pptx`。
- 将当前最新的 `Q2/Q3/Q4/Q5` HTML 页面转成图片，并作为新的引导页 / 讲解页插入总 PPT。
- 新建一个完整的第二幕汇报总 `pptx`：
  - 包含封面页；
  - 包含问题 1 到问题 6 的过渡页；
  - 保留同学原始 PPT 页面不改内容，尽量原样并入；
  - 由主持人视角把整套汇报串成一个合理顺序，尤其解决 `Q4` 多位同学内容的排序。

## 目标拆解
1. 读取同学 PPT 目录，提取每个文件的：
   - 文件名、页数、每页标题或首要文本；
   - 初步对应的问题编号（Q1~Q6）；
   - 是否属于 Q4 内部机制 / 文献 / 图示 / 病例落地等不同子段。
2. 确定总汇报顺序：
   - 主线按问题 1 到 6 排；
   - `Q4` 内部按“现象界定 → 总开关 → 生成程序 → 分化失衡 → 出髓异常 → 前沿异质性 → 同学补充文献/图示/延展”的教学逻辑排序；
   - 在排序说明中明确“为什么这样串最顺”。
3. 生成新的自制 PPT 页面：
   - 标题页 1 张；
   - 各问题过渡页（Q1~Q6）若干；
   - 必要时补一个“Q4 串讲导览页”；
   - 自己制作的 `Q2/Q3/Q4/Q5` HTML 截图页转为 PNG，并插入总 PPT。
4. 合并成总 `pptx`：
   - 优先采用 PowerPoint 自动化方式复制同学原始页，以尽量保留动画和原生对象；
   - 自制页用 PowerPoint 原生页或由 PptxGenJS 先生成后并入；
   - 输出一个新的完整汇报 `pptx`。
5. 做结果检查：
   - 检查页序、页数、问题顺序和分节页是否正确；
   - 检查自制截图页是否清晰、比例是否正确；
   - 更新 `PROGRESS.md` 并提交本轮代码 / 脚本 / 成品构建逻辑。

## 执行约束
- 不修改同学原始 `pptx` 的内容和版式，只做汇总与插入顺序安排。
- 新增的标题页、分节页、衔接页要与当前自制 HTML 的风格一致。
- 尽量保留动画；如果某种技术路径会丢失同学页动画，则优先改用更能保留原始页的路径。

## 当前状态
- 已完成：已确认 `同学ppt` 目录存在 9 份 `pptx` 文件；已确认本机安装了 Microsoft PowerPoint，可尝试用 COM 自动化做原样合并；当前自制 `Q2/Q3/Q4/Q5` 静态截图版 HTML 已可作为总 PPT 的图片来源。
- 正在做：读取同学 PPT 的页数和页面主题信息，并准备设计总汇报顺序。
- 下一步：先盘点同学页主题与题号归属，再决定最终串讲顺序和合并方案。

# ExecPlan

## 任务
- 以 `血液与肿瘤/PBL作业/Case1-第二幕/PPT-最终整合版.html` 为主稿，拆分出按“切页算一页”的独立 HTML：
  - `Q2` 保留 1 个文件；
  - `Q3` 保留 1 个文件；
  - `Q5` 保留 1 个文件；
  - `Q4` 按 3 页拆成 3 个文件。
- 调整整合版中 `Q2` 与 `Q4` 的页面布局，只做纵向拉伸、纵向间距调整和元素位置微调，不改变既有视觉风格。
- 以 `血液与肿瘤/PBL作业/Case1-第二幕/Case1-第二幕.md` 为事实真源，修正 PPT 中与报告不一致或存在科学性风险的内容。

## 目标拆解
1. 读取整合版 HTML，梳理每个 `slide` 的内容、脚本依赖和可复用的通用样式。
2. 对照 `Case1-第二幕.md` 重点核查：
   - `Q2` 的现代分类、CML 在 MPN 中的位置、表述是否与报告一致；
   - `Q4` 三页机制内容与报告是否一致，尤其是“现象界定”“四层机制/五层视角”的逻辑。
3. 修改整合版排版：
   - `Q2` 将“现代精细分类”内容挪回首屏下半区，填补原先四大型下方大空白；
   - `Q4-1` 放大“现象界定”区字号和竖向留白；
   - `Q4-2` 只向下拉伸绿色、黄色两块及其内部排版，填补下方空白，不做横向整体放大。
4. 生成独立 HTML 文件：
   - 每个文件保留 16:9 画布、自适应缩放、页脚和必要动画；
   - `Q4` 三页分别成为 3 个文件，避免再依赖切页到下一页。
5. 执行检查：
   - 用脚本或命令验证 HTML 结构数量、标题与页数是否符合要求；
   - 检查关键文本是否与 Markdown 报告一致；
   - 更新 `PROGRESS.md` 并提交本轮修改。

## 执行约束
- 只改动本次拆分与排版修正直接相关的文件，不回退用户已有修改。
- 保持当前配色、标题栏、页脚和整体信息层级，不擅自改风格。
- 所有布局调整优先用局部尺寸、行距、间距与容器高度处理，不做横向扩张式放大。

## 当前状态
- 已完成：已确认整合版共有 Q2、Q3、Q5 各 1 页，Q4 共 3 页；已确认现有 `PPT-问题2/3/4/5.html` 属于旧版分稿，不能直接当作本次拆分结果；已找到本次需要对齐的事实来源 `Case1-第二幕.md`。
- 正在做：更新计划并准备修改整合版布局。
- 下一步：先改整合版，再产出新的独立 HTML 文件并做检查。

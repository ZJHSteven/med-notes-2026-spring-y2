# 项目状态快照

## 当前结论（必须最新）
- 现状：22 个已修改或新增文件已按 7 门课程完成分组提交，当前只剩最终推送步骤。
- 已完成：已确认当前分支为 `main`，远端为 `origin`，已完成 22 个文件的课程分组统计，已提交计划与进度初始化记录，并已完成 `医学人文英语`、`局部解剖学`、`心理危机干预与预防`、`毛思想`、`病毒-分子生物学工具与疾病`、`细生实验`、`血液与肿瘤` 共 7 组课程提交。
- 正在做：同步最终状态记录并准备推送到远端仓库。
- 下一步：执行 `git push origin main`，然后再次检查工作区是否干净。

## 关键决策与理由（防止“吃书”）
- 决策A：按课程而不是按文件提交。（原因：课程目录天然对应知识模块，后续回看历史更容易定位。）
- 决策B：先补 `PLANS.md` 与 `PROGRESS.md` 再开始正式提交。（原因：遵守仓库协作规范，避免后续任务状态不连续。）

## 常见坑 / 复现方法
- 坑1：`git status` 对未跟踪目录只显示目录名，不会直接展开里面的文件。（复现：新建目录后直接运行 `git status --short`。）
- 坑2：Windows 默认 Git 可能转义中文路径，阅读状态时需要关闭 `quotepath`。（复现：直接运行未设置 `core.quotepath=false` 的状态命令。）

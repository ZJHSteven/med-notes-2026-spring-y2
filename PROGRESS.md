# 项目状态快照

## 当前结论（必须最新）
- 现状：第二幕 PPT 已拆成静态截图版单题 HTML；当前进入“汇总全班第二幕总 PPT”阶段，需要把自制页和同学原始页合并成完整 `pptx`。
- 已完成：已在 `血液与肿瘤/PBL作业/Case1-第二幕/` 下新增通用 `build_html.py` 与 `style.css`；已成功生成 `Case1-第二幕-美化版.html`、`Case1-第二幕-美化版.pdf`；已验证导语保留、块引用黄框、总结标题适配、第一幕 Markdown 兼容输入、A4 PDF 导出链路；已去掉问题块边界上与 `.qa-section` 结构分隔重复的 Markdown 分割线；已将 `.summary-text` 从黄框样式中拆出，只保留 `.callout` 使用黄色引用框；已补上 PDF 被占用时自动落到“更新版”文件名的兜底逻辑；已确认本轮需要把整合版拆成 `Q2/Q3/Q5` 单文件与 `Q4` 三个独立文件，并以 `Case1-第二幕.md` 为事实真源进行校对；已将整合版 `Q2` 改为同页承载“传统四大型 + 现代精细分类”；已把 `Q5` 中不符合报告的“未见巨核细胞”修正为“巨核细胞减少”；已新增 `PPT-通用样式.css` 与 `PPT-单页脚本.js` 作为拆分后的公共底座；已完成 `PPT-问题2.html`、`PPT-问题3.html`、`PPT-问题5.html` 与 `PPT-问题4.html`、`PPT-问题4-2.html`、`PPT-问题4-3.html` 六个静态截图版文件；已根据用户反馈再次压缩 `Q2` 信息层级，解决下半区内容被切掉的问题；已将 `Q4-2`、`Q4-3` 的内容扩写到接近报告原文的信息密度，并补入病例落点；已把这些新版内容同步回 `PPT-最终整合版.html`；已确认 `同学ppt` 目录下共有 9 份同学原始 `pptx`，且本机安装了 Microsoft PowerPoint，可用于后续原样合并。
- 正在做：提取 9 份同学 PPT 的页数、题号归属和页面主题信息，并设计问题 1 到 6 的总顺序与 `Q4` 内部最合理串讲顺序。
- 下一步：完成素材盘点后，生成总 PPT 的封面页 / 过渡页 / 自制截图页，并合并成新的完整 `pptx`。

## 关键决策与理由（防止“吃书”）
- 决策A：脚本层面重写为通用版，但视觉层面继续沿用第一幕既有 CSS。（原因：第二幕结构变了，逻辑必须重写；但用户明确希望页面观感保持不变。）
- 决策B：CSS 只追加第二幕新结构需要的最少类名，不整体重做配色和排版。（原因：这样能最大限度保证第一幕与第二幕风格一致，避免“修功能时把样式改味了”。）

## 常见坑 / 复现方法
- 坑1：旧脚本会跳过“先说我觉得最重要的机制问题”这类前导段落。（复现：运行第一幕旧脚本后查看问题 4 之前的导语，页面中不会出现。）
- 坑2：旧脚本把总结区标题写死成“总结第一幕分析结论”，无法适配“本幕总结（当前阶段结论）”。（复现：将第二幕 Markdown 直接交给旧脚本运行。）
- 坑3：Edge 无头打印直接输出到中文 PDF 文件名时，可能不报错但不落盘。（复现：将 `--print-to-pdf` 指向 `Case1-第二幕-美化版.pdf` 这类中文文件名。）
- 坑4：单独依赖 Edge 命令行 `--print-to-pdf` 不够稳定，而 Playwright 的 `page.pdf()` 更可靠。（复现：分别测试两种导出方式，对比落盘成功率。）
- 坑5：若“关键词”后面先跟 `---` 再跟导语，不能按“直到下一个同级标题”为止直接截关键词，否则导语会被误并入关键词。（复现：第二幕与第一幕的关键词区都能复现。）
- 坑6：如果某个问题块正文最后正好以 `---` 结束，再叠加 HTML 里的 `.qa-section` 顶部分隔，就会出现双重分隔。（复现：问题 1/2、2/3 等相邻问题边界都能看到。）

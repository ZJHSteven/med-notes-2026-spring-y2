# 项目状态快照

## 当前结论（必须最新）
- 现状：正在为 `Case1-第二幕` 搭建一套通用的 Markdown 转 HTML/PDF 流程，旧版第一幕脚本已确认存在结构写死与内容吞段问题。
- 已完成：已检查第一幕 `build_html.py`、`style.css`、第二幕 Markdown 结构，并确认第二幕采用新的行内链接写法，新增了块引用与“本幕总结（当前阶段结论）”标题。
- 正在做：同步本次任务的 `PLANS.md` 与 `PROGRESS.md`，准备开始脚本与样式改造。
- 下一步：在 `血液与肿瘤/PBL作业/Case1-第二幕/` 下新增通用 `build_html.py` 与 `style.css`，随后生成并验证 HTML/PDF。

## 关键决策与理由（防止“吃书”）
- 决策A：不直接继续沿用第一幕的硬编码脚本，而是在第二幕目录重写成可传参、可复用的通用版。（原因：第二幕的标题、总结段、链接格式和前导段落规则都已经变化，继续打补丁会更脆弱。）
- 决策B：HTML 解析优先使用 Python 标准库自定义规则，PDF 导出再接入官方浏览器引擎。（原因：Markdown 结构是课程作业的固定样式，标准库足以覆盖；而 PDF 打印背景与 A4 版式需要浏览器渲染能力来保证一致性。）

## 常见坑 / 复现方法
- 坑1：旧脚本会跳过“先说我觉得最重要的机制问题”这类前导段落。（复现：运行第一幕旧脚本后查看问题 4 之前的导语，页面中不会出现。）
- 坑2：旧脚本把总结区标题写死成“总结第一幕分析结论”，无法适配“本幕总结（当前阶段结论）”。（复现：将第二幕 Markdown 直接交给旧脚本运行。）

# 项目状态快照

## 当前结论（必须最新）
- 现状：第二幕通用转换脚本已开始落地，样式策略已改为“直接沿用第一幕 CSS，只补第二幕新增结构必须的少量类名”。
- 已完成：已检查第一幕 `build_html.py`、`style.css`、第二幕 Markdown 结构；已新增第二幕目录下的通用 `build_html.py`；已明确用户要求不要改整体视觉风格。
- 正在做：把第一幕 `style.css` 原样迁移到第二幕目录，并只补 `callout`、导语区、四级标题等最低限度样式。
- 下一步：运行脚本生成第二幕 HTML/PDF，并做结构、样式与导出验证。

## 关键决策与理由（防止“吃书”）
- 决策A：脚本层面重写为通用版，但视觉层面继续沿用第一幕既有 CSS。（原因：第二幕结构变了，逻辑必须重写；但用户明确希望页面观感保持不变。）
- 决策B：CSS 只追加第二幕新结构需要的最少类名，不整体重做配色和排版。（原因：这样能最大限度保证第一幕与第二幕风格一致，避免“修功能时把样式改味了”。）

## 常见坑 / 复现方法
- 坑1：旧脚本会跳过“先说我觉得最重要的机制问题”这类前导段落。（复现：运行第一幕旧脚本后查看问题 4 之前的导语，页面中不会出现。）
- 坑2：旧脚本把总结区标题写死成“总结第一幕分析结论”，无法适配“本幕总结（当前阶段结论）”。（复现：将第二幕 Markdown 直接交给旧脚本运行。）
